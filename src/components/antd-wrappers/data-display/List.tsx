/**
 * List wrapper for Ant Design 6.0
 * 
 * Direct re-export from antd - no customization needed
 * Preserves all sub-components (List.Item, List.Item.Meta)
 * 
 * @example
 * ```tsx
 * <List
 *   dataSource={data}
 *   renderItem={item => (
 *     <List.Item>
 *       <List.Item.Meta title={item.title} />
 *     </List.Item>
 *   )}
 * />
 * ```
 */
export { List } from 'antd';
export type { ListProps } from 'antd';
