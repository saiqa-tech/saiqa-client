/**
 * Checkbox wrapper for Ant Design 6.0
 * 
 * Direct re-export from antd - no customization needed
 * Preserves all sub-components (Checkbox.Group)
 * 
 * @example
 * ```tsx
 * <Checkbox>Checkbox Label</Checkbox>
 * <Checkbox.Group options={options} />
 * ```
 */
export { Checkbox } from 'antd';
export type { CheckboxProps } from 'antd';
