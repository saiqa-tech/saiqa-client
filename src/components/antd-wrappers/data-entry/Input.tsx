/**
 * Input wrapper for Ant Design 6.0
 * 
 * Direct re-export from antd - no customization needed
 * Preserves all sub-components (Input.Password, Input.TextArea, Input.Search, etc.)
 * 
 * @example
 * ```tsx
 * <Input placeholder="Enter text" />
 * <Input.Password placeholder="Enter password" />
 * <Input.TextArea rows={4} />
 * ```
 */
export { Input } from 'antd';
export type { InputProps, InputRef } from 'antd';
