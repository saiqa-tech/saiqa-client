/**
 * Grid wrapper for Ant Design 6.0
 * 
 * Direct re-export from antd - provides responsive grid system
 * Includes useBreakpoint hook
 * 
 * @example
 * ```tsx
 * const screens = Grid.useBreakpoint();
 * ```
 */
export { Grid } from 'antd';
